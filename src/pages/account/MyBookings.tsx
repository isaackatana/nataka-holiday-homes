import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export default function MyBookings() {
  return <PagePlaceholder title="My bookings" note="Protected route — built in Step 12." />
}
