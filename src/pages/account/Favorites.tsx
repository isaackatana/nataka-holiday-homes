import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export default function Favorites() {
  return <PagePlaceholder title="Your favorites" note="Protected route — built in Step 11." />
}
