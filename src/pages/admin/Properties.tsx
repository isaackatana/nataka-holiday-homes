import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export default function Properties() {
  return <PagePlaceholder title="Admin — Properties" note="Property list — built in Step 13/14." />
}
